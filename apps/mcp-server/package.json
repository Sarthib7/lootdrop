{
  "name": "@lootdrop/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests in mcp package\"",
    "start": "node --import tsx --env-file=../../.env src/index.ts"
  },
  "dependencies": {
    "@lootdrop/core": "workspace:*",
    "@lootdrop/db": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
